# DocMaster — Python 后端依赖清单
# Windows 11 环境，Python 3.10+

# === Web 框架 ===
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0

# === 文档处理核心库 ===
pdf2docx>=0.5.8          # PDF → Word 转换（排版还原度最高）
python-pptx>=0.6.23       # PPT 创建与读取
python-docx>=1.1.0        # Word 文档创建与读取
pypdf>=4.2.0              # PDF 合并/拆分（PyPDF2 继任者）
Pillow>=10.3.0            # 图片处理（PDF 转 PPT 时需要）

# === 可选依赖（需要系统安装 LibreOffice） ===
# 如果不安装 LibreOffice，Word→PDF 和 PPT→PDF 将使用替代方案：
# - Word→PDF：python-docx 读取 + 前端浏览器打印
# - PPT→PDF：python-pptx 读取 + 前端浏览器打印
# 推荐安装 LibreOffice 以获得最佳转换效果：
# 下载地址：https://www.libreoffice.org/download/download/
