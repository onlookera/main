[phases.install]
cmds = [
  "apt-get update -qq",
  "apt-get install -y -qq libreoffice-writer libreoffice-impress fonts-liberation fonts-wqy-zenhei fonts-dejavu-core",
  "fc-cache -f -v > /dev/null 2>&1",
  "rm -rf /var/lib/apt/lists/*",
]
