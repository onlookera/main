{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -m pip install -r requirements.txt -q --no-cache-dir && python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8080}",
    "restartPolicyType": "ALWAYS",
    "numReplicas": 1
  }
}
